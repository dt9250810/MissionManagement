<%= form_for(mission) do |f| %>
  <%= f.label :title, t("activerecord.attributes.car_premium.mission.title") %>
  <%= f.text_field :title %> <br /><br />

  <%= f.label :context, t("activerecord.attributes.car_premium.mission.context") %>
  <%= f.text_area :context %> <br />

  <%= f.submit '確認' %>
<% end %>