<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <%= link_to '任務管理', root_path, class: 'navbar-brand' %>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNav">
    <ul class="navbar-nav">
      <li class="nav-item">
        <%= link_to "任務列表", user_missions_path(current_user), class: 'nav-link' if current_user %>
      </li>
      <li class="nav-item">
        <%= link_to "新增任務", new_user_mission_path(current_user), class: 'nav-link' if current_user %>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="#">Pricing</a>
      </li>
      <li class="nav-item">
        <a class="nav-link disabled" href="#">Disabled</a>
      </li>
    </ul>
    <div class="ml-auto">
        <ul class="navbar-nav">
        <% if current_user %>
          <% if current_user.is_admin %>
            <li class="nav-item">
              <%= link_to "管理帳戶", '#', class: 'nav-link' %>
            </li>
          <% end %>
          <li class="nav-item">
            哈囉, <%= current_user.name %>  |  <%= link_to '登出', logout_path, method: :delete %>
          </li>
          
        <% else %>
          <%= link_to 'Login', login_path %>  |  <%= link_to 'Sign up', signup_path %>
        <% end %>
      </ul>
    </div>
  </div>
</nav>